<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MApplication"
        android:allowBackup="true"
        android:largeHeap="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._3DModelSample"
        tools:targetApi="31">
        <activity
            android:name=".SettingActivity"
            android:exported="false" />
        <activity
            android:name=".MusicActivity"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
            android:exported="true">
            <intent-filter>

                <!-- <action android:name="android.intent.action.MAIN" /> -->

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".MyCarBroadcastReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.kanavi.heyari.ACTION_OPEN" />
                <action android:name="com.kanavi.heyari.ACTION_CLOSE" />
                <action android:name="com.kanavi.heyari.ACTION_ON" />
                <action android:name="com.kanavi.heyari.ACTION_OFF" />
                <action android:name="com.kanavi.heyari.ACTION_DOWN" />
                <action android:name="com.kanavi.heyari.ACTION_UP" />
                <action android:name="com.kanavi.heyari.ACTION_ROTATE" />
                <action android:name="com.kanavi.heyari.ACTION_ZOOMIN" />
                <action android:name="com.kanavi.heyari.ACTION_ZOOMOUT" />
                <action android:name="com.kanavi.heyari.ACTION_UNKNOWN" />
            </intent-filter>
        </receiver>
    </application>

</manifest>